import RegisterForm from '@/components/Register'
import React from 'react'

export const metadata = {
  title: 'Register',
}

const page = () => {
  return (
   <RegisterForm />
  )
}

export default page